// hàm filter duyệt qua 1 mảng và tạo ra từ đó 1 mảng mới trả ra 1 mảng có các giá trị là true

//function trong 1 hàm được gọi là callback

arrListProduct.filter(function (item) {return id==1})

//hàm find duyệt qua 1 mảng và trả về 1 giá trị đầu tiên có giá trị true
arrListProduct.find(function)
//

function check () {
    let value = document.querySelector(".test").value;
    if(value == "abc") {
        console.log("Đúng")
    }
}

//button onclick = check